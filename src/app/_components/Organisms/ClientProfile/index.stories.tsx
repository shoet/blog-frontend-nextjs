import { Meta, StoryObj } from "@storybook/react";
import { ClientProfile } from ".";

export default {
  title: "Organisms/ClientProfile",
  component: ClientProfile,
} as Meta<typeof ClientProfile>;

export type Story = StoryObj<typeof ClientProfile>;

export const Default: Story = {};
